<div class="page-header">
  <h1><%= @album.title %></h1>
</div>

<ul class="thumbnails">
<% @images.each do |image| %>
  <li>
    <%= link_to image_tag(image.attachment.url(:thumb)), image, :class => "thumbnail" %>
  </li>
<% end %>
</ul>